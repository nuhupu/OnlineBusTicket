@model IEnumerable<OnlineBusTicket.Models.BusSchedule>
@{
    ViewBag.Title = "BusSchedule";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<!-- Exportable Table -->
<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <div class="header">
                <h2>
                    Bus Schedule Management
                </h2>
                <ul class="header-dropdown m-r--5">
                    <li class="dropdown">
                        <a href="@Url.Action("AddBusSchedule", "Admin")" role="button" aria-haspopup="true" aria-expanded="false">
                            <i class="material-icons">add_box</i>
                        </a>

                    </li>
                </ul>
            </div>
            <div class="body">
                <div class="table-responsive">
                    <table class="table table-bordered table-striped table-hover dataTable js-exportable">
                        <thead>
                            <tr>

                                <th>Bus number</th>
                                <th>Start Place</th>
                                <th>Destination</th>
                                <th>Start Time</th>
                                <th>Price</th>
                                <th>Edit</th>
                            </tr>
                        </thead>
                        <tfoot>
                            <tr>
                                <th>Bus number</th>
                                <th>Start Place</th>
                                <th>Destination</th>
                                <th>Start Time</th>
                                <th>Price</th>
                                <th>Edit</th>

                            </tr>
                        </tfoot>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    @{
                                        foreach (var a in ViewBag.CounterName)
                                        {
                                            if (Convert.ToInt32(a.Value) == item.StartPlace)
                                            {
                                                <td>@a.Text</td>
                                            }
                                        }
                                        foreach (var a in ViewBag.CounterName)
                                        {
                                            if (Convert.ToInt32(a.Value) == item.Destination)
                                            {
                                                <td>@a.Text</td>
                                            }
                                        }
                                    }
                                    <td>@Html.DisplayFor(modelItem => item.StartTime)</td>
                                    <td>@Html.DisplayFor(modelItem => item.btPrice)</td>

                                    <td><a href="@Url.Action("Edit", "Admin", new { id = item.btId })"><i class="material-icons">mode_edit</i></a></td>
                                </tr>
                            }


                            @*<tr>
            <td>00001</td>
            <td>Sai Gon</td>
            <td>Vung Tau</td>
            <td>6:00</td>
            <td>$10</td>
            <td><i class="material-icons">mode_edit</i></td>
        </tr>
*@
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- #END# Exportable Table -->
